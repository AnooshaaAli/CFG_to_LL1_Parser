#include <stdio.h>
#include <stdlib.h>

// reads cfg from file
void readCfGfromFile(const char* filename) {
    FILE *file = fopen()
}

int main() {
    char* filename;
    readCFGfromFile(filename);
    return 0;
}
