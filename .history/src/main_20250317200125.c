#include <stdio.h>
#include <stdlib.h>

// reads cfg from file
void readCfGfromFile(char* filename) {
    FILE *file
}

int main() {
    char* filename;
    readCFGfromFile(filename);
    return 0;
}
